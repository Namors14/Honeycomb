import { Profile } from '../../privateComponents/Profile';
import { Settings } from '../../privateComponents/Settings';

export default {
    Profile: {
        component: Profile,
        path: '/profile'
    },
    Settings: {
        component: Settings,
        path: '/settings'
    },
    
};